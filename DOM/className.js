/**
className: The className property represents the class attribute of an element as a string. It allows you to get or set the class names assigned to an element. If an element has multiple classes, they are separated by spaces within the string.
*/

const element = document.getElementById('myDiv');
// console.log(element);

// console.log(element.className); // "red bold"
// console.log(element);




// element.className = 'blue';
// console.log(element.className); // "blue"
// console.log(element);



// element.className = 'white';
// console.log(element.className); // "white"
// console.log(element);


element.className = 'purple semi-bold';
console.log(element.className); // "purple semi-bold"
console.log(element);
